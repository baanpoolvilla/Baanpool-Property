# Logic การนับ % ความคืบหน้า — ข้อมูลบ้านพูลวิลล่า (Chatbot Data Quality Score)

> เวอร์ชัน: 1.0 | วัตถุประสงค์: ติดตามการทำงาน Admin + เพิ่มประสิทธิภาพ Chatbot

---

## หลักการออกแบบ

| หลักการ | รายละเอียด |
|---|---|
| **Weighted Score** | แต่ละ field มีน้ำหนักต่างกันตามความสำคัญต่อ Chatbot |
| **Quality-based** | Text field วัดจากจำนวนตัวอักษร ไม่ใช่แค่ "มีหรือไม่มี" |
| **Conditional Skip** | Field ที่ขึ้นกับ toggle (เช่น ไม่มีสระ → ข้าม section สระ) |
| **Proportional Checklist** | สิ่งอำนวยความสะดวกคิดตามสัดส่วนที่ติ๊ก |
| **คะแนนรวม 100%** | แบ่งเป็น 3 ระดับ: Critical / Important / Nice to Have |

---

## ภาพรวมคะแนน

| ระดับ | รวม | Section |
|---|---|---|
| 🔴 Critical | **45%** | ข้อมูลทั่วไป, ความจุ, กฎ/ข้อปฏิบัติ |
| 🟠 Important | **35%** | ที่ตั้ง, สระ, ที่จอดรถ, สิ่งอำนวยความสะดวก |
| 🟡 Nice to Have | **20%** | รายละเอียดห้อง, ทีนอนเสริม, ร้านใกล้เคียง |

---

## Section 1 — ข้อมูลทั่วไป 🔴 Critical (15%)

### 1.1 รหัสบ้าน
- **น้ำหนัก:** 2%
- **เงื่อนไข:** มีค่า (ไม่ว่าง)
- **Min length:** 2 ตัวอักษร
- **Logic:**
  ```
  score += (รหัสบ้าน.length >= 2) ? 2 : 0
  ```

---

### 1.2 ชื่อที่พัก
- **น้ำหนัก:** 5%
- **เงื่อนไข:** มีค่า + ความยาวเพียงพอ
- **Min length:** 5 ตัวอักษร (เช่น "HS2-อัตเดน สเปซ พูลวิลล่า")
- **ทำไม:** Chatbot ต้องเรียกชื่อบ้านได้ถูกต้องในทุก response
- **Logic:**
  ```
  if (ชื่อที่พัก.length >= 5)     score += 5
  else if (ชื่อที่พัก.length > 0)  score += 2   // partial
  else                             score += 0
  ```

---

### 1.3 รายละเอียดที่พัก (Description หลัก)
- **น้ำหนัก:** 8%
- **เงื่อนไข:** ต้องมี text ครอบคลุมภาพรวมบ้าน
- **Min length:** 80 ตัวอักษร
- **Good length:** 150–400 ตัวอักษร
- **ทำไม:** นี่คือข้อมูลหลักที่ Chatbot ใช้แนะนำบ้านให้ลูกค้า ถ้าสั้นเกินไปบอทจะตอบได้แค่ผิวเผิน
- **ตัวอย่างดี:**
  ```
  รายละเอียดห้อง
  4 ห้องนอน 4 ห้องน้ำ
  ห้องที่ 1 เตียง 5 ฟุต 1 เตียง (ไม่มีห้องน้ำ)
  ห้องที่ 2 เตียง 5 ฟุต 2 เตียง (มีห้องน้ำ)
  ห้องที่ 3 เตียง 5 ฟุต 2 เตียง (มีห้องน้ำ)
  ห้องที่ 4 เตียง 5 ฟุต 1 เตียง (มีห้องน้ำ)
  ห้องน้ำส่วนกลาง 1 ห้อง
  ```
- **Logic:**
  ```
  len = รายละเอียดที่พัก.length
  if (len >= 150)     score += 8
  else if (len >= 80) score += 5
  else if (len >= 30) score += 2
  else                score += 0
  ```

---

### 1.4 ค่าประกันบ้าน
- **น้ำหนัก:** 3% (ใน Critical เพราะลูกค้าถามก่อนโอนเงิน)
- **เงื่อนไข:** ตัวเลข > 0
- **Logic:**
  ```
  score += (ค่าประกัน > 0) ? 3 : 0
  ```

---

### 1.5 วันที่อัพเดตภาพล่าสุด
- **น้ำหนัก:** 1%
- **เงื่อนไข:** มีวันที่
- **Logic:**
  ```
  score += (วันที่อัพเดตภาพ != null) ? 1 : 0
  ```

---

## Section 2 — ที่ตั้ง / แผนที่ 🟠 Important (12%)

### 2.1 โซน / พื้นที่
- **น้ำหนัก:** 3%
- **เงื่อนไข:** เลือกค่าจาก dropdown
- **ทำไม:** Chatbot ใช้กรองบ้านตามโซน
- **Logic:**
  ```
  score += (โซน != null && โซน != "") ? 3 : 0
  ```

---

### 2.2 ลิงก์ Google Maps
- **น้ำหนัก:** 4%
- **เงื่อนไข:** มี URL ที่ขึ้นต้นด้วย `http` และมีความยาว > 10 ตัวอักษร
- **ทำไม:** ลูกค้าขอ location บ่อยมาก และ Chatbot ส่ง link ได้ตรง ๆ
- **Logic:**
  ```
  score += (googleMapsUrl.startsWith("http") && googleMapsUrl.length > 10) ? 4 : 0
  ```

---

### 2.3 ระยะห่างจากทะเล
- **น้ำหนัก:** 2%
- **เงื่อนไข:** ตัวเลข >= 0
- **Logic:**
  ```
  score += (ระยะทะเล >= 0) ? 2 : 0
  ```

---

### 2.4 ชื่อทะเล/หาด
- **น้ำหนัก:** 1%
- **Min length:** 3 ตัวอักษร
- **Logic:**
  ```
  score += (ชื่อทะเล.length >= 3) ? 1 : 0
  ```

---

### 2.5 ประเภทการติดทะเล (Checkbox group)
- **น้ำหนัก:** 2%
- **เงื่อนไข:** เลือกอย่างน้อย 1 ตัวเลือก
- **ทำไม:** ลูกค้าที่อยากได้ Beachfront ต้องการข้อมูลนี้
- **Logic:**
  ```
  score += (ติดทะเล_options.some(o => o.checked)) ? 2 : 0
  ```

---

### 2.6 ร้านสะดวกซื้อ / ร้านอาหารใกล้เคียง
- **น้ำหนัก:** 0% (ย้ายไป Nice to Have) → ดูหัวข้อ 7

---

## Section 3 — ความจุ / พื้นที่ใช้สอย 🔴 Critical (18%)

### 3.1 รองรับผู้เข้าพักสูงสุด
- **น้ำหนัก:** 7%
- **เงื่อนไข:** ตัวเลข > 0
- **ทำไม:** คำถามแรกของลูกค้าเกือบทุกราย
- **Logic:**
  ```
  score += (รองรับสูงสุด > 0) ? 7 : 0
  ```

---

### 3.2 จำนวนห้องนอน
- **น้ำหนัก:** 3%
- **เงื่อนไข:** ตัวเลข > 0
- **Logic:**
  ```
  score += (จำนวนห้องนอน > 0) ? 3 : 0
  ```

---

### 3.3 จำนวนห้องน้ำ (รวม)
- **น้ำหนัก:** 2%
- **เงื่อนไข:** ตัวเลข > 0
- **Logic:**
  ```
  score += (จำนวนห้องน้ำ > 0) ? 2 : 0
  ```

---

### 3.4 ห้องน้ำในตัว (จำนวน)
- **น้ำหนัก:** 1%
- **เงื่อนไข:** ตัวเลข >= 0 (0 ก็ถือว่าระบุแล้ว)
- **Logic:**
  ```
  score += (ห้องน้ำในตัว != null) ? 1 : 0
  ```

---

### 3.5 ห้องน้ำส่วนกลาง (จำนวน)
- **น้ำหนัก:** 1%
- **เงื่อนไข:** ตัวเลข >= 0
- **Logic:**
  ```
  score += (ห้องน้ำส่วนกลาง != null) ? 1 : 0
  ```

---

### 3.6 จำนวนชั้น
- **น้ำหนัก:** 1%
- **เงื่อนไข:** ตัวเลข > 0
- **Logic:**
  ```
  score += (จำนวนชั้น > 0) ? 1 : 0
  ```

---

### 3.7 มีทีนอนเสริม (Toggle)
- **น้ำหนัก:** 1%
- **เงื่อนไข:** toggle ถูก set (ไม่ว่าจะ on/off)
- **Logic:**
  ```
  score += (มีทีนอนเสริม != null) ? 1 : 0
  ```

---

### 3.8 รายละเอียดทีนอนเสริม (ถ้า toggle = ON)
- **น้ำหนัก:** 2% — **Conditional** (นับเฉพาะถ้า toggle เปิด)
- **Min length:** 20 ตัวอักษร (เช่น "ฟูกทีนอน 3 ฟุต สามารถเสริมได้สูงสุด 2 ท่าน")
- **Logic:**
  ```
  if (มีทีนอนเสริม == true) {
    score += (รายละเอียดทีนอนเสริม.length >= 20) ? 2 : 0
  } else {
    score += 2   // ถ้าไม่มีทีนอนเสริม ไม่หักแต้ม
  }
  ```

---

## Section 4 — สระว่ายน้ำ 🟠 Important (10%)

> **Conditional Section:** ถ้า toggle "มีสระว่ายน้ำ" = OFF → ได้ 10% อัตโนมัติ (ระบุชัดเจนแล้ว)

### Logic หลักของ Section:
```
if (มีสระ == false) {
  score += 10   // ระบุชัดว่าไม่มี = ข้อมูลครบ
} else {
  // คำนวณ sub-fields ด้านล่าง
}
```

---

### 4.1 Toggle "มีสระว่ายน้ำ"
- **น้ำหนัก:** 2%
- **Logic:**
  ```
  score += 2   // แค่ set toggle = ได้คะแนน (on หรือ off)
  ```

---

### 4.2 ประเภทน้ำในสระ
- **น้ำหนัก:** 2%
- **เงื่อนไข:** เลือกจาก dropdown (เกลือ / คลอรีน ฯลฯ)
- **Logic:**
  ```
  score += (ประเภทน้ำสระ != null && ประเภทน้ำสระ != "") ? 2 : 0
  ```

---

### 4.3 ขนาดสระ
- **น้ำหนัก:** 2%
- **Min length:** 3 ตัวอักษร (เช่น "5x8")
- **Logic:**
  ```
  score += (ขนาดสระ.length >= 3) ? 2 : 0
  ```

---

### 4.4 ความลึกสระ (ขั้นต่ำ + สูงสุด)
- **น้ำหนัก:** 1%
- **เงื่อนไข:** ทั้ง 2 ค่า > 0
- **Logic:**
  ```
  score += (ความลึกต่ำ > 0 && ความลึกสูงสุด > 0) ? 1 : 0
  ```

---

### 4.5 เวลาเปิด-ปิดสระ
- **น้ำหนัก:** 2%
- **เงื่อนไข:** มีทั้งเวลาเปิดและเวลาปิด (format HH:MM)
- **ทำไม:** ลูกค้าถามว่าเล่นน้ำได้ถึงกี่โมง
- **Logic:**
  ```
  score += (เวลาเปิดสระ != "" && เวลาปิดสระ != "") ? 2 : 0
  ```

---

### 4.6 มีเสื้อชูชีพให้ (Toggle)
- **น้ำหนัก:** 1%
- **Logic:**
  ```
  score += (มีเสื้อชูชีพ != null) ? 1 : 0
  ```

---

## Section 5 — ที่จอดรถ 🟠 Important (6%)

### 5.1 จำนวนที่จอดรถสูงสุด
- **น้ำหนัก:** 2%
- **เงื่อนไข:** ตัวเลข >= 0 (0 = ไม่มี ก็ถือว่าระบุแล้ว)
- **Logic:**
  ```
  score += (จำนวนที่จอดสูงสุด != null) ? 2 : 0
  ```

---

### 5.2 จอดรถในบ้าน
- **น้ำหนัก:** 2%
- **เงื่อนไข:** ตัวเลข >= 0
- **Logic:**
  ```
  score += (จอดในบ้าน != null) ? 2 : 0
  ```

---

### 5.3 จอดรถหน้าบ้าน / ถนน
- **น้ำหนัก:** 2%
- **เงื่อนไข:** ตัวเลข >= 0
- **Logic:**
  ```
  score += (จอดหน้าบ้าน != null) ? 2 : 0
  ```

> **หมายเหตุ:** มีที่ชาร์จ EV — **ยกเว้น** ไม่นับในคะแนน (ตามที่ระบุไว้)

---

## Section 6 — สิ่งอำนวยความสะดวก 🟠 Important (10%)

### Logic: Proportional Checklist
- **น้ำหนัก:** 10% (คิดตามสัดส่วนที่ติ๊ก)
- **Base minimum:** ต้องติ๊กอย่างน้อย 5 รายการจึงเริ่มได้คะแนนเต็มสัดส่วน
- **Total checklist items:** ~40 รายการ (ยกเว้น EV)

```
checkedCount = จำนวน checkbox ที่ติ๊ก
totalItems = 40

if (checkedCount == 0)          score += 0
else if (checkedCount <= 5)     score += 2   // เริ่มติ๊ก แต่ยังน้อย
else                            score += min(10, (checkedCount / totalItems) * 10)
```

### รายการ Checklist ที่สำคัญต่อ Chatbot มากที่สุด (ถ้าไม่มีจะถูกลดลำดับ):
| รายการ | ทำไมสำคัญ |
|---|---|
| กาน้ำร้อน | ลูกค้าถามบ่อย |
| การเชื่อมต่ออินเทอร์เน็ต | Top 3 คำถาม |
| ครัวในบ้าน (เตาไฟฟ้า) | กลุ่มครอบครัวถามมาก |
| เครื่องปรับอากาศ | สำคัญมากสำหรับเมืองร้อน |
| ตู้เย็น | ถามบ่อย |
| สระ | ควรสอดคล้องกับ section สระ |
| ระบบเสียง | ลูกค้ากลุ่มปาร์ตี้ถาม |
| สายฉีดชำระ | ลูกค้าไทยถามมาก |

---

## Section 7 — กฎ / ข้อปฏิบัติ 🔴 Critical (12%)

### 7.1 กฎระเบียบบ้านพัก
- **น้ำหนัก:** 8%
- **Min length:** 100 ตัวอักษร
- **Good length:** 300–800 ตัวอักษร
- **ทำไม:** นี่คือข้อมูลที่ป้องกันปัญหาข้อพิพาท Chatbot ต้องอ่านกฎได้ถูกต้อง
- **Quality levels:**

| ความยาว | คะแนน | ความหมาย |
|---|---|---|
| 0 | 0 | ไม่มีกฎ — อันตรายมาก |
| 1–49 | 2 | มีกฎน้อยมาก |
| 50–99 | 4 | พอมีบ้าง แต่ไม่ครบ |
| 100–199 | 6 | ใช้ได้ระดับพื้นฐาน |
| 200+ | 8 | ครบถ้วน — เต็ม |

```
len = กฎระเบียบ.trim().length
if (len >= 200)      score += 8
else if (len >= 100) score += 6
else if (len >= 50)  score += 4
else if (len >= 1)   score += 2
else                 score += 0
```

---

### 7.2 เวลาเช็คอิน
- **น้ำหนัก:** 2%
- **เงื่อนไข:** มีค่า format HH:MM หรือ HH.MM
- **Logic:**
  ```
  score += (เวลาเช็คอิน != "" && เวลาเช็คอิน != null) ? 2 : 0
  ```

---

### 7.3 เวลาเช็คเอาท์
- **น้ำหนัก:** 2%
- **Logic:**
  ```
  score += (เวลาเช็คเอาท์ != "" && เวลาเช็คเอาท์ != null) ? 2 : 0
  ```

---

### 7.4 อนุญาตสัตว์เลี้ยง (Toggle)
- **น้ำหนัก:** 0.5%
- **Logic:**
  ```
  score += (อนุญาตสัตว์เลี้ยง != null) ? 0.5 : 0
  ```

---

### 7.5 รายละเอียดค่าใช้จ่ายสัตว์เลี้ยง (ถ้า toggle = ON)
- **น้ำหนัก:** 0.5% — **Conditional**
- **Min length:** 10 ตัวอักษร
- **Logic:**
  ```
  if (อนุญาตสัตว์เลี้ยง == true) {
    score += (ค่าใช้จ่ายสัตว์.length >= 10) ? 0.5 : 0
  } else {
    score += 0.5   // ระบุว่าไม่อนุญาต = ครบ
  }
  ```

---

### 7.6 ค่าเช็คอินก่อนเวลา / หลังเวลา
- **น้ำหนัก:** 0.5% (รวม 2 field = 0.5%)
- **เงื่อนไข:** ตัวเลข > 0 หรือ = 0 (0 = ฟรี ก็ถือว่าระบุ)
- **Logic:**
  ```
  score += (ค่าก่อนเวลา != null && ค่าหลังเวลา != null) ? 0.5 : 0
  ```

---

### 7.7 ห้ามส่งเสียงดังหลังเวลา (เวลา)
- **น้ำหนัก:** 0.5%
- **Logic:**
  ```
  score += (ห้ามเสียงดังหลัง != "" && ห้ามเสียงดังหลัง != null) ? 0.5 : 0
  ```

---

## Section 8 — Nice to Have 🟡 (20%)

### 8.1 รายละเอียดแต่ละห้องนอน (sub-fields)
- **น้ำหนัก:** 8% รวม (2% ต่อห้อง × 4 ห้อง)
- **เงื่อนไข ต่อห้อง:** มีข้อมูล อย่างน้อย ขนาดเตียง + จำนวนเตียง
- **Min:** ต้องมี tag อย่างน้อย 1 อัน (เช่น "เตียงควีนไซซ์ 5 ฟุต")
- **Logic:**
  ```
  bedrooms.forEach(room => {
    hasData = room.bedSize != null || room.bedCount > 0
    score += hasData ? (8 / totalBedrooms) : 0
  })
  ```

---

### 8.2 ร้านสะดวกซื้อ / ร้านอาหารใกล้เคียง
- **น้ำหนัก:** 5%
- **เงื่อนไข:** มีอย่างน้อย 1 รายการ
- **Bonus:** มี 3+ รายการ = เต็ม 5%
- **Logic:**
  ```
  count = ร้านใกล้เคียง.length
  if (count >= 3)     score += 5
  else if (count >= 1) score += 3
  else                 score += 0
  ```

---

### 8.3 รายละเอียดห้องน้ำส่วนกลาง
- **น้ำหนัก:** 2%
- **เงื่อนไข:** ถ้ามีห้องน้ำส่วนกลาง → ต้องมีข้อมูลชั้น
- **Logic:**
  ```
  if (ห้องน้ำส่วนกลาง > 0) {
    score += (ชั้นห้องน้ำ != null) ? 2 : 0
  } else {
    score += 2   // ไม่มี = ข้อมูลครบ
  }
  ```

---

### 8.4 อนุญาตสูบบุหรี่ภายนอก (Toggle)
- **น้ำหนัก:** 1%
- **Logic:**
  ```
  score += (อนุญาตสูบบุหรี่นอก != null) ? 1 : 0
  ```

---

### 8.5 ระยะทางร้านใกล้เคียง (เดินได้ toggle)
- **น้ำหนัก:** 1%
- **เงื่อนไข:** มีข้อมูลระยะทาง km ใน field
- **Logic:**
  ```
  score += ร้านใกล้เคียง.some(r => r.ระยะ > 0) ? 1 : 0
  ```

---

### 8.6 วันที่อัพเดตภาพล่าสุด (ซ้ำจาก Section 1 แต่นับแยก Quality)
- **ยกเลิก** — นับที่ Section 1 แล้ว

---

### 8.7 พื้นที่ทำอาหารกลางแจ้ง / พื้นที่ทำงาน (Checklist bonus)
- **นับรวมใน Section 6 แล้ว**

---

## สรุปตาราง Field ทั้งหมด + น้ำหนัก

| # | Field | Section | น้ำหนัก | Min Length | หมายเหตุ |
|---|---|---|---|---|---|
| 1 | รหัสบ้าน | ทั่วไป | 2% | 2 chars | — |
| 2 | ชื่อที่พัก | ทั่วไป | 5% | 5 chars | Chatbot ใช้เรียกชื่อ |
| 3 | รายละเอียดที่พัก | ทั่วไป | 8% | 80 chars | Quality-based |
| 4 | ค่าประกัน | ทั่วไป | 3% | > 0 | ตัวเลข |
| 5 | วันที่อัพเดตภาพ | ทั่วไป | 1% | มีค่า | — |
| 6 | โซน/พื้นที่ | ที่ตั้ง | 3% | dropdown | — |
| 7 | Google Maps link | ที่ตั้ง | 4% | URL valid | — |
| 8 | ระยะห่างทะเล | ที่ตั้ง | 2% | >= 0 | — |
| 9 | ชื่อทะเล/หาด | ที่ตั้ง | 1% | 3 chars | — |
| 10 | ประเภทติดทะเล | ที่ตั้ง | 2% | เลือก ≥1 | checkbox group |
| 11 | รองรับสูงสุด | ความจุ | 7% | > 0 | สำคัญที่สุด |
| 12 | จำนวนห้องนอน | ความจุ | 3% | > 0 | — |
| 13 | จำนวนห้องน้ำรวม | ความจุ | 2% | > 0 | — |
| 14 | ห้องน้ำในตัว | ความจุ | 1% | >= 0 | — |
| 15 | ห้องน้ำส่วนกลาง | ความจุ | 1% | >= 0 | — |
| 16 | จำนวนชั้น | ความจุ | 1% | > 0 | — |
| 17 | Toggle ทีนอนเสริม | ความจุ | 1% | set | — |
| 18 | รายละเอียดทีนอน | ความจุ | 2% | 20 chars | Conditional |
| 19 | Toggle มีสระ | สระ | 2% | set | — |
| 20 | ประเภทน้ำสระ | สระ | 2% | dropdown | Conditional |
| 21 | ขนาดสระ | สระ | 2% | 3 chars | Conditional |
| 22 | ความลึกสระ | สระ | 1% | > 0 | Conditional |
| 23 | เวลาเปิด-ปิดสระ | สระ | 2% | HH:MM | Conditional |
| 24 | เสื้อชูชีพ | สระ | 1% | set | Conditional |
| 25 | จำนวนที่จอดสูงสุด | จอดรถ | 2% | >= 0 | — |
| 26 | จอดในบ้าน | จอดรถ | 2% | >= 0 | — |
| 27 | จอดหน้าบ้าน | จอดรถ | 2% | >= 0 | — |
| 28 | Checklist สิ่งอำนวย | อำนวยความสะดวก | 10% | proportional | — |
| 29 | กฎระเบียบ | กฎ | 8% | 100 chars | Quality-based |
| 30 | เวลาเช็คอิน | กฎ | 2% | HH:MM | — |
| 31 | เวลาเช็คเอาท์ | กฎ | 2% | HH:MM | — |
| 32 | อนุญาตสัตว์เลี้ยง | กฎ | 0.5% | set | — |
| 33 | ค่าสัตว์เลี้ยง | กฎ | 0.5% | 10 chars | Conditional |
| 34 | ค่า early/late check | กฎ | 0.5% | >= 0 | — |
| 35 | ห้ามเสียงหลังเวลา | กฎ | 0.5% | HH:MM | — |
| 36 | รายละเอียดห้องนอน | Nice | 8% | tag ≥ 1 | per room |
| 37 | ร้านใกล้เคียง | Nice | 5% | ≥ 1 ร้าน | — |
| 38 | รายละเอียดห้องน้ำกลาง | Nice | 2% | ชั้น | Conditional |
| 39 | อนุญาตสูบบุหรี่ | Nice | 1% | set | — |
| 40 | ระยะทางร้าน | Nice | 1% | > 0 | — |

**รวม: 100%** ✅ (ยกเว้น EV Charger ตามที่กำหนด)

---

## Logic หลัก (Pseudocode รวม)

```javascript
function calculateProgress(data) {
  let score = 0

  // === Section 1: ทั่วไป (19%) ===
  if (data.รหัสบ้าน?.length >= 2)           score += 2
  score += qualityScore(data.ชื่อที่พัก, [
    { min: 5,  pts: 5 },
    { min: 1,  pts: 2 },
  ])
  score += qualityScore(data.รายละเอียด, [
    { min: 150, pts: 8 },
    { min: 80,  pts: 5 },
    { min: 30,  pts: 2 },
  ])
  if (data.ค่าประกัน > 0)                    score += 3
  if (data.วันที่อัพเดต)                      score += 1

  // === Section 2: ที่ตั้ง (12%) ===
  if (data.โซน)                              score += 3
  if (data.googleMaps?.startsWith("http"))    score += 4
  if (data.ระยะทะเล >= 0)                    score += 2
  if (data.ชื่อหาด?.length >= 3)             score += 1
  if (data.ประเภทติดทะเล.some(o => o))       score += 2

  // === Section 3: ความจุ (18%) ===
  if (data.รองรับสูงสุด > 0)                 score += 7
  if (data.ห้องนอน > 0)                      score += 3
  if (data.ห้องน้ำรวม > 0)                   score += 2
  if (data.ห้องน้ำในตัว != null)             score += 1
  if (data.ห้องน้ำกลาง != null)              score += 1
  if (data.ชั้น > 0)                          score += 1
  if (data.toggleทีนอน != null)              score += 1
  if (data.toggleทีนอน === true) {
    score += (data.รายละเอียดทีนอน?.length >= 20) ? 2 : 0
  } else { score += 2 }

  // === Section 4: สระ (10%) ===
  if (data.มีสระ === false) {
    score += 10
  } else {
    score += 2   // toggle set
    if (data.ประเภทน้ำสระ)                   score += 2
    if (data.ขนาดสระ?.length >= 3)           score += 2
    if (data.ลึกต่ำ > 0 && data.ลึกสูง > 0) score += 1
    if (data.เวลาเปิดสระ && data.เวลาปิดสระ) score += 2
    if (data.มีเสื้อชูชีพ != null)           score += 1
  }

  // === Section 5: จอดรถ (6%) ===
  if (data.จอดสูงสุด != null)               score += 2
  if (data.จอดในบ้าน != null)               score += 2
  if (data.จอดหน้าบ้าน != null)             score += 2

  // === Section 6: Checklist (10%) ===
  const checked = data.checklist.filter(c => c).length
  const total = data.checklist.length
  if (checked === 0)         score += 0
  else if (checked <= 5)     score += 2
  else score += Math.min(10, (checked / total) * 10)

  // === Section 7: กฎ (12%) ===
  score += qualityScore(data.กฎระเบียบ, [
    { min: 200, pts: 8 },
    { min: 100, pts: 6 },
    { min: 50,  pts: 4 },
    { min: 1,   pts: 2 },
  ])
  if (data.เวลาเช็คอิน)                      score += 2
  if (data.เวลาเช็คเอาท์)                    score += 2
  if (data.toggleสัตว์ != null) {
    score += 0.5
    if (data.toggleสัตว์ === true) {
      score += (data.ค่าสัตว์?.length >= 10) ? 0.5 : 0
    } else { score += 0.5 }
  }
  if (data.ค่าก่อนเวลา != null && data.ค่าหลังเวลา != null) score += 0.5
  if (data.ห้ามเสียงหลัง)                   score += 0.5

  // === Section 8: Nice to Have (20%) ===
  const roomScore = data.ห้องนอนDetail.filter(r => r.tags?.length > 0).length
  score += (roomScore / data.ห้องนอนDetail.length) * 8

  const shops = data.ร้านใกล้เคียง.length
  if (shops >= 3)     score += 5
  else if (shops >= 1) score += 3

  if (data.ห้องน้ำกลาง > 0) {
    score += (data.ชั้นห้องน้ำกลาง != null) ? 2 : 0
  } else { score += 2 }

  if (data.toggleสูบบุหรี่ != null) score += 1
  if (data.ร้านใกล้เคียง.some(r => r.ระยะ > 0)) score += 1

  return Math.round(score)
}

// Helper: คืนคะแนนตาม quality tier
function qualityScore(text, tiers) {
  const len = (text ?? "").trim().length
  for (const tier of tiers.sort((a, b) => b.min - a.min)) {
    if (len >= tier.min) return tier.pts
  }
  return 0
}
```

---

## เกณฑ์การแสดงผลให้ Admin

| % | สถานะ | สีแสดงผล | action แนะนำ |
|---|---|---|---|
| 0–30% | ❌ ข้อมูลไม่เพียงพอ | แดง | แจ้ง Admin เร่งด่วน |
| 31–59% | ⚠️ ข้อมูลบางส่วน | เหลือง | ระบุ field ที่ยังขาด |
| 60–79% | 🔵 ใช้ได้ระดับพื้นฐาน | น้ำเงิน | แนะนำเพิ่มเติม |
| 80–94% | 🟢 ดี | เขียว | — |
| 95–100% | ✅ สมบูรณ์ | เขียวเข้ม | พร้อม deploy bot |

---

## Dashboard สำหรับ Admin (แนะนำ)

```
[ชื่อบ้าน]   BS52 — HS2-อัตเดน สเปซ พูลวิลล่า
[คะแนนรวม]  87%  ████████████████░░░  

Critical    : 43/45  ✅
Important   : 32/35  ✅
Nice to Have: 12/20  ⚠️

⚠️ Field ที่ยังขาด:
  - ร้านใกล้เคียง (มีแค่ 2 ร้าน ควร ≥ 3)
  - รายละเอียดห้องนอนที่ 1 (ยังไม่มี tag)
```

---

*เอกสารนี้ควร Review ทุก 3 เดือน เมื่อมี field ใหม่เพิ่มเข้าระบบ*
